annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
click==8.3.3
fastapi==0.136.1
greenlet==3.5.0
h11==0.16.0
httptools==0.7.1
idna==3.14
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
SQLAlchemy==2.0.49
sqlmodel==0.0.38
starlette==1.0.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.46.0
uvloop==0.22.1
watchfiles==1.1.1
websockets==16.0
httpx
python-multipart==0.0.32
# Argon2id password hashing (passwords.py). cffi and pycparser come with it and
# are deliberately NOT pinned: this repo's venv runs Python 3.14 and the image
# runs 3.11, and the versions with wheels differ between them. Pinning the two
# top-level packages is enough to keep hashing behaviour fixed.
argon2-cffi==25.1.0
argon2-cffi-bindings==26.1.0
